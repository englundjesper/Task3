package items;

import heroes.HeroFigure;

import java.util.Scanner;

public class Armor implements Items {

    /*
    @Override
    public void chooseArmor(HeroFigure holder) {
            System.out.println("1. Leather");
            System.out.println("2. Plate");
            System.out.println("3. Cloth");

        Scanner s = new Scanner(System.in);

        int decision = s.nextInt();

            if (decision == 1) {
                decideGearSlot();

                int nextChoice = s.nextInt();

                switch (nextChoice) {
                    case 1:
                        slotOption = slotOption.Head;
                        displayArmorAttributes(ArmorOption.Leather, holder, slotOption);
                        break;
                    case 2:
                        slotOption = slotOption.Body;
                        displayArmorAttributes(ArmorOption.Leather, holder, slotOption);
                        break;
                    case 3:
                        slotOption = slotOption.Legs;
                        displayArmorAttributes(ArmorOption.Leather, holder, slotOption);
                        break;
                }
            } else if (decision == 2){
                decideGearSlot();

                int nextDecision = s.nextInt();

                switch (nextDecision) {
                    case 1:
                        slotOption = slotOption.Head;
                        displayArmorAttributes(ArmorOption.Plate, holder, slotOption);
                        break;
                    case 2:
                        slotOption = slotOption.Body;
                        displayArmorAttributes(ArmorOption.Plate, holder, slotOption);
                        break;
                    case 3:
                        slotOption = slotOption.Legs;
                        displayArmorAttributes(ArmorOption.Plate, holder, slotOption);
                        break;
                }
            } else if (decision == 3){
                decideGearSlot();

                int nextDecision = s.nextInt();

                switch (nextDecision) {
                    case 1:
                        slotOption = slotOption.Head;
                        displayArmorAttributes(ArmorOption.Cloth, holder, slotOption);
                        break;
                    case 2:
                        slotOption = slotOption.Body;
                        displayArmorAttributes(ArmorOption.Cloth, holder, slotOption);
                        break;
                    case 3:
                        slotOption = slotOption.Legs;
                        displayArmorAttributes(ArmorOption.Cloth, holder, slotOption);
                        break;
                }
            } else {
                System.out.println("Error occurred");
            }
        }


 public void decideGearSlot() {
     System.out.println("Choose gearslot!");
     System.out.println("Head");
     System.out.println("Body");
     System.out.println("Legs");
 }

     */

    @Override
    public void generateLevel() {

    }

    @Override
    public void generateNames() {

    }

    @Override
    public void createItems() {

    }

    @Override
    public void chooseArmor() {

    }
}
